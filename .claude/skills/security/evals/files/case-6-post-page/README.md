# blogapp — post page slice

`client/` is the Vite + React reader UI, `server/` is the Express app that serves the API.
The client is served from `blogapp.example`, the API from `api.blogapp.example`.
