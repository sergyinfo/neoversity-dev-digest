# specs — mcp

Agreed requirements / acceptance criteria for the `mcp` package, one folder per tool
surface, holding a numbered series: `<tool>/NN-<slug>.md`. Written by `/spec`; read by
`implementation-planner`. Empty for now.
