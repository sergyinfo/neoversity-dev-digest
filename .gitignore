node_modules/
dist/
# agent-runner ships as a JS GitHub Action — its bundled dist/ MUST be committed
# (GitHub runs action.yml `main: dist/index.js` as-is, with no build step).
!agent-runner/dist/
!agent-runner/dist/**
build/
.next/
out/
coverage/
*.tsbuildinfo
.env
.env.local
.DS_Store
.idea/
.turbo/
.pnpm-store/
*.log
.devdigest/cache/
clones/
playwright-report/
test-results/

# emdash worktree config (local)
.emdash.json

# Skills are part of the repo. Some machines carry `.claude/skills` in their
# global gitignore, which would silently drop new skills from commits while the
# ones added earlier stay tracked — a repo .gitignore beats core.excludesFile,
# so re-include the directory here.
!.claude/skills/

# Agent instructions live in AGENTS.md. CLAUDE.md is a LOCAL symlink to it,
# created by scripts/link-agents.sh (also run by scripts/dev.sh) — never
# committed, so a Windows clone without Developer Mode can't end up with a
# one-line text file where the symlink should be.
CLAUDE.md
