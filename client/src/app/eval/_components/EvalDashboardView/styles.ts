import type { CSSProperties } from "react";

/** Co-located styles for `/eval` — the Eval Dashboard (L06). */
export const s = {
  page: { padding: "20px 28px 40px", maxWidth: 980, margin: "0 auto" } satisfies CSSProperties,
  headerRow: { display: "flex", alignItems: "flex-end", marginBottom: 18 } satisfies CSSProperties,
  h1: { fontSize: 22, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  subtitle: { fontSize: 13, color: "var(--text-secondary)", marginTop: 3 } satisfies CSSProperties,
  backBtn: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    marginBottom: 12,
    padding: "4px 8px 4px 4px",
    borderRadius: 6,
    border: "none",
    background: "transparent",
    color: "var(--text-secondary)",
    fontSize: 12.5,
    fontWeight: 600,
    cursor: "pointer",
  } satisfies CSSProperties,
  sectionGap: { marginTop: 24, marginBottom: 8 } satisfies CSSProperties,
  agentList: { display: "flex", flexDirection: "column", gap: 8, marginBottom: 24 } satisfies CSSProperties,
  agentRow: {
    display: "flex",
    alignItems: "center",
    gap: 16,
    padding: "14px 16px",
    borderRadius: 10,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    cursor: "pointer",
    textAlign: "left",
    width: "100%",
  } satisfies CSSProperties,
  agentIcon: {
    width: 34,
    height: 34,
    borderRadius: 8,
    background: "var(--accent-bg)",
    color: "var(--accent)",
    display: "grid",
    placeItems: "center",
    flexShrink: 0,
  } satisfies CSSProperties,
  agentBody: { minWidth: 0, flex: 1 } satisfies CSSProperties,
  agentName: { display: "flex", alignItems: "center", gap: 8 } satisfies CSSProperties,
  agentMeta: { fontSize: 11.5, color: "var(--text-muted)", marginTop: 3 } satisfies CSSProperties,
  mini: { textAlign: "center", minWidth: 60 } satisfies CSSProperties,
  miniLabel: {
    fontSize: 9.5,
    fontWeight: 700,
    letterSpacing: "0.04em",
    color: "var(--text-muted)",
    textTransform: "uppercase",
  } satisfies CSSProperties,
  miniValue: { fontSize: 17, fontWeight: 700, marginTop: 2 } satisfies CSSProperties,
  table: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    overflow: "hidden",
    background: "var(--bg-elevated)",
    marginTop: 8,
  } satisfies CSSProperties,
  tableHead: {
    display: "grid",
    gap: 10,
    padding: "9px 14px",
    background: "var(--bg-surface)",
    borderBottom: "1px solid var(--border)",
    fontSize: 10.5,
    fontWeight: 700,
    letterSpacing: "0.04em",
    color: "var(--text-muted)",
    textTransform: "uppercase",
  } satisfies CSSProperties,
  tableRow: {
    display: "grid",
    gap: 10,
    padding: "10px 14px",
    alignItems: "center",
    fontSize: 12.5,
    borderTop: "1px solid var(--border)",
  } satisfies CSSProperties,
  metricsRow: { display: "flex", gap: 14, marginBottom: 20 } satisfies CSSProperties,
  chartCard: { marginBottom: 20 } satisfies CSSProperties,
  chartHeader: { display: "flex", alignItems: "center", gap: 16, marginBottom: 12 } satisfies CSSProperties,
  legend: { marginLeft: "auto", display: "flex", gap: 14, fontSize: 11.5 } satisfies CSSProperties,
  legendItem: { display: "inline-flex", alignItems: "center", gap: 5, color: "var(--text-secondary)" } satisfies CSSProperties,
  legendSwatch: { width: 10, height: 2, borderRadius: 2 } satisfies CSSProperties,
  runsHeader: { display: "flex", alignItems: "center", gap: 10, marginBottom: 4 } satisfies CSSProperties,
  selHint: { fontSize: 11.5, color: "var(--text-muted)" } satisfies CSSProperties,
  checkboxCell: { display: "flex", alignItems: "center" } satisfies CSSProperties,
} as const;
