import { SkillsListView } from "./_components/SkillsListView";

/* Route: /skills (L02 Skills Lab). Thin route entry — the grid, import row and
   styles are colocated under _components/SkillsListView. */
export default function SkillsPage() {
  return <SkillsListView />;
}
